// Create an object called laptop with types for brand (string), ram (number), and isSSD (boolean).

let laptop : {
    brand: string;
    ram: number;
    isSSD: boolean;
}={
    brand: "Dell",
    ram: 32,
    isSSD: true
}